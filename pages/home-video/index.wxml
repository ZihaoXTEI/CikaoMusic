<!-- 视频主页 -->

<view class="video">
  <view class="item" wx:for="{{topMVs}}" wx:key="id">
    <video-item
      item="{{item}}"
      bindtap="handleVideoItemTap"
      data-item="{{item}}">
    </video-item>
  </view>
</view>